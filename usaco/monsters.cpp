#include <iostream>
#include <bits/stdc++.h>
#include <limits.h>

using namespace std;
typedef long long ll;
typedef vector<int> vi;
typedef pair<int, int> ii;
using ld = long double;
using ll = long long;

const int SIZE = 1000;

int n, m, lab[SIZE][SIZE];

vector<int> bfs() {
}

// Driver program to test above functions
int main()
{
    cin >> n >> m;
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < m; j++) {
            chat c; cin >> c;
            if(c == '#')
    return 0;
}
