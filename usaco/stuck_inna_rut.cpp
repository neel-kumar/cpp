#include <bits/stdc++.h>
#include <utility>

using namespace std;

int n, ans[1000], minx = INT8_MAX, miny = INT8_MAX, maxx, maxy;
vector<pair<int, pair<int, char>>> x;
vector<pair<int, pair<int, char>>> y;
map<double, int> coords2ind;
map<int, vector<int>> g;

int main() {
    cin >> n;

    return 0;
}
